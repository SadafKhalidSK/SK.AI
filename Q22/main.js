"use strict";
//Intentional Error: If you haven’t received an array index error in one of your programs yet, try to make one happen.///
// Change an index in one of your programs to produce an index error.
// Make sure you correct the error before closing the program.
let days = ['Sunday', 'Monday', 'Tuesdy', 'Wednesday'];
console.log("INDEX ERROR:", days[0], days[1], days[2], days[3], days[4]);
// correction of error
let days = ['Sunday', 'Monday', 'Tuesdy', 'Wednesday'];
console.log("Correction of error:", days[0], days[1], days[2], days[3]);
