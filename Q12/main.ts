//Greetings: Start with the array you used in Exercise 11,
//  but instead of just printing each person’s name, print a message to them.
//   The text of each message should be the same, but each message should be 
//   personalized with the person’s name.

let friendsName=['Dania','Iffat','Humaira']
friendsName.forEach(friends =>
    console.log(`"Hi, ${friends} how are you ?"`))

     
    let friendsNames=['Dania','Iffat','Humaira']
    console.log(friendsNames[0])
        console.log(friendsNames[1])

            console.log(friendsNames[2])
            //             //......map...checking of map{}
// let friendsNamesMap:string[]=["a","s"]
//     friendsNamesMap.map((items) =>(
//     console.log({items})));
        
